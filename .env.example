# Optional: fine-grained GitHub token with read-only access to ChangNoi79/NextGenCapture.
# The public API works without it; use the secret in CI for reliable scheduled builds.
NEXTGEN_CAPTURE_READ_TOKEN=
